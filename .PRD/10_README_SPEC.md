# 소담그래프엔지니어링 (SoDam-Graph-Eng) — README 작성 규격

> **이 문서는 `README.md` 자체가 아니라, `README.md` 를 어떻게 써야 하는지에 대한 요구사항입니다.**
> 작성 시점: **M8** (Phase 1 맨 마지막 — 다 만든 뒤에 써야 실제와 안 어긋남)
> 생성일: 2026-08-02

---

## 0. 독자 정의 — 이게 규격의 전부입니다

**대상 독자**

- 개발(코딩)을 **한 번도 해본 적 없는 사람**
- AI·메신저·컴퓨터·전자기기·IT 기기를 **처음 다루는 사람**
- 비개발자 · 비전문가 · 일반인

**따라서 아래를 전제하지 않습니다.**

| 전제하지 않는 것 | 대신 |
|----------------|------|
| 터미널·명령 프롬프트를 안다 | **어디를 눌러서 여는지**부터 씀 |
| git·Node.js가 뭔지 안다 | **"이게 뭔지"를 한 줄로** 설명하고 설치 링크를 줌 |
| "저장소"·"의존성"·"훅"이 뭔지 안다 | **일상어로 풀어 쓰고** 괄호에 원어 병기 |
| 에러 메시지를 읽을 줄 안다 | **화면에 뜬 문장 그대로** 찾을 수 있게 §17에 목록화 |
| 경로를 직접 고칠 줄 안다 | **복사해서 붙여넣을 수 있는 형태**로 제공 |

> 🔴 **성공 기준**: `03_PHASES.md` M8의 *"처음 보는 사람이 README만 보고 설치 성공"*.
> **판정 방법**: 이 프로젝트를 모르는 상태에서 README만 보고 따라 해봅니다(**새 세션**).

---

## 1. 필수 목차 (19개 — 순서 고정)

| # | 섹션 | 왜 이 순서인가 |
|---|------|-------------|
| 1 | **목차** | 긴 문서에서 길을 잃지 않게. 앵커 링크 필수 |
| 2 | **이게 뭔가요?** (3줄) | 설치 전에 "나에게 필요한가" 판단 |
| 3 | **사전 준비물** | 없으면 설치가 실패하므로 설치보다 앞 |
| 4 | **필요 프로그램** | 준비물의 구체 목록 + 버전 + 확인 방법 |
| 5 | **다운로드 방법** | — |
| 6 | **설치 방법** | — |
| 7 | **빠른 시작** (5분) | 설치 직후 성공 경험 먼저 |
| 8 | **실행 방법** | — |
| 9 | **사용 방법** | 시나리오별 |
| 10 | **작동 방법**(원리) | "왜 이렇게 되는지" — 안 읽어도 되게 |
| 11 | **명령어 목록** | 표 1개 |
| 12 | **업데이트 내용 요약** | **토글(`<details>`) 형태** |
| 13 | **파일·문서 위치** | 뭐가 어디 있는지 |
| 14 | **워크플로우** | 하루 사용 흐름 |
| 15 | **아키텍처** | 그림 1장 |
| 16 | **보안·데이터 흐름** | 무엇을 읽고 어디에 쓰는지 |
| 17 | **문제·오류 대처** | **가장 두꺼워야 하는 섹션** |
| 18 | **FAQ** | — |
| 19 | **법률·저작권·라이선스·상업적 용도** | `09` L-5 내용 |

---

## 2. 섹션별 작성 규격

### §3 사전 준비물 · §4 필요 프로그램

**표 하나로, 확인 명령까지 함께.**

| 필요한 것 | 왜 필요한가 | 확인 방법 | 없으면 |
|----------|-----------|----------|--------|
| **Claude Code** | 이 도구는 Claude Code 안에서 도는 플러그인입니다 | `claude --version` | 설치 링크 |
| **git** | 형제 프로젝트의 진행 상황을 읽는 데 씁니다 | `git --version` | 설치 링크 |
| **Node.js** | 이 도구가 Node.js로 만들어졌습니다 | `node --version` | 설치 링크 |
| **소담 형제 폴더** | 이 도구가 읽을 대상입니다 | 폴더가 있는지 눈으로 확인 | §17 참조 |

**규격**

- 각 프로그램에 **최소 버전** 명시 + **버전이 낮으면 어떤 증상**인지 한 줄
- **Windows 기준**으로 쓰되 macOS·Linux 차이가 있으면 열 추가
- 🔴 **git은 Windows에 기본 설치되어 있지 않습니다** — 왕초보가 가장 많이 막히는 지점이라 **굵게**
- 확인 명령을 **어디에 입력하는지**(터미널 여는 법)를 §3 첫머리에 1~2줄

### §5~§6 다운로드·설치 방법 — 🆕 규격 신설 (2026-08-02)

> **초안은 목차에 §5·§6을 넣어놓고 작성 규격이 없었습니다.** 형제 하네스 `README.md` 의 **7단계 절차**(실측)를 왕초보용으로 풀어 씁니다.

**필수 구성**

| # | 내용 |
|---|------|
| 1 | **7단계 번호 절차** — `04_PROJECT_SPEC.md` §설치 방법과 **한 글자도 어긋나면 안 됨** |
| 2 | **설치 경로 2가지를 나란히**: **(A) GitHub ← 표준** / (B) 내 컴퓨터 폴더 ← (A) 실패 시 폴백 |
| 3 | **명령 형식을 통째로 복붙 가능하게** — `/plugin marketplace add sodam-ai/SoDam-Graph-Eng` → `/plugin install sodam-graph@sodamgraph-marketplace`. 🔴 **`플러그인@마켓플레이스` 결합 형식이 필수**라고 한 줄 경고(앞부분만 쓰면 설치 안 됨) |
| 3-a | **제거 방법도 함께** — `/plugin uninstall sodam-graph@sodamgraph-marketplace` → `/plugin marketplace remove sodamgraph-marketplace` (재설치 테스트에 필요) |
| 4 | 폴더 경로를 **복사해서 붙여넣을 수 있는 형태**로 제공 |
| 5 | 🔴 **6단계(완전 재시작)를 굵게 + 방법까지**: `/exit` → 터미널 창 닫기 → 새 터미널 → `claude`. **가장 많이 빠뜨리는 단계**(`§17-1`) |
| 6 | 7단계 성공 확인: `/sodam-graph:` 를 치면 **명령 7개가 뜬다** — 무엇이 보여야 정상인지 화면 예시 |
| 7 | 각 단계마다 **실패 시 갈 곳** 링크 (`→ 안 되면 §17-4`) |

**용어 풀이 (왕초보 기준 — 처음 나올 때 괄호로)**

- 마켓플레이스(플러그인을 모아둔 목록) · 플러그인(Claude Code에 기능을 더하는 꾸러미) · 재시작(프로그램을 완전히 껐다 켜기)

### §7 빠른 시작 (5분)

**설치 직후 성공 경험을 만드는 것이 목적입니다.**

```
1. 설치한다
2. Claude Code를 완전히 껐다 켠다          ← 이거 안 하면 명령이 안 보임
3. /graph-where 라고 쳐본다
4. 형제 이름과 폴더 위치가 나오면 성공
```

- **4단계 이내**
- 각 단계에 **예상 화면 출력**(뭐가 나와야 정상인지)
- **실패 시 바로 갈 곳** 링크 (`→ 안 되면 §17-1`)

### §11 명령어 목록

| 명령 | 뭘 하나 | 언제 쓰나 |
|------|--------|----------|
| `/graph-where` | 지금 어느 프로젝트이고 폴더가 어디인지 | 헷갈릴 때 |
| `/graph-next` | 7형제 각각의 다음 할 일 | 뭐부터 할지 볼 때 |
| `/graph-why` | **딱 하나만 고르라면 이것** | 결정을 못 하겠을 때 |
| `/graph-map` | 7형제 관계 그림 | 전체를 보고 싶을 때 |
| `/graph-shadow` | 정보가 부족한 프로젝트를 채워 넣음 | `coarse` 라고 뜰 때 |
| `/graph-reject` | "아직 안 끝났어" 라고 알려줌 | 완료 제안이 틀렸을 때 |
| `/graph-undo` | 이미 완료된 걸 되돌림 | 실수로 완료됐을 때 |

- **명령 이름을 외우게 하지 않습니다** — "이럴 때 이거"로 찾게
- 각 명령에 **실제 출력 예시** 1개씩

### §12 업데이트 내용 요약 — **토글 형태 필수**

```markdown
<details>
<summary><b>v0.1.0 (2026-08-__) — 첫 버전</b></summary>

- 7형제 지도와 다음 할 일 알려주기
- 폴더 이름이 바뀌어도 찾아내기
</details>
```

- **최신 버전이 맨 위**, 기본 **접힌 상태**
- 각 항목은 **"무엇이 좋아졌는지"** 를 사용자 말로 (내부 용어 금지)
- 🔴 **깨는 변경**이 있으면 **접기 밖에 경고**로 노출

### §15 아키텍처 — 그림 1장 + 3줄

```
[7형제 폴더] --읽기만--> [소담그래프엔지니어링] --요약--> [~/.sodam/graph-state.json]
                              |                                    |
                          graph.json                        6형제가 읽어감
                        (계획·정본)
```

- **글자보다 그림 우선** (Mermaid 또는 위 같은 ASCII)
- "읽기만"을 **화살표에 직접 표기** — 안전하다는 걸 그림에서 바로 알게

### §16 보안·데이터 흐름 — 왕초보가 가장 불안해하는 부분

**질문에 답하는 형식으로.**

| 독자의 불안 | 답할 내용 |
|-----------|----------|
| "내 다른 프로젝트를 망가뜨리나요?" | **아니요.** 읽기만 합니다. 확인 방법: `git status` 전후 비교(명령 제공) |
| "인터넷으로 뭘 보내나요?" | **아니요.** 서버·외부 전송 없음 |
| "비밀번호나 키를 읽나요?" | **아니요.** `.env`·키 파일은 **읽지 않도록 막아** 뒀습니다 (`08` S-4) |
| "어디에 파일을 만드나요?" | 두 곳뿐 — 이 프로그램 폴더 안, `~/.sodam/graph-state.json` |
| "지우고 싶으면?" | §17-17 |

- `08_SECURITY_SPEC.md` 의 **결론만** 옮기고 기술 용어는 뺍니다

### §17 문제·오류 대처 — **가장 두꺼운 섹션**

**규격**: 화면에 뜨는 문구를 **그대로** 제목에 넣습니다. 사용자는 메시지를 복사해서 찾습니다.
각 항목은 **증상 → 원인 → 해결** 3단. 아래는 **반드시 포함해야 하는 최소 목록**입니다.

#### 설치 단계

| # | 증상 | 원인 | 해결 |
|---|------|------|------|
| 17-1 | **명령을 쳤는데 반응이 없다 / `/graph-` 명령이 목록에 없다** | **Claude Code 재시작 안 함**(가장 흔함) | **완전히 종료 후 재실행.** 창만 닫는 게 아니라 프로그램을 끝내야 함 |
| 17-2 | `git: command not found` / `'git'은(는) 내부 또는 외부 명령이 아닙니다` | git 미설치 (**Windows 기본 미설치**) | git 설치 후 **터미널 재시작** |
| 17-3 | `node: command not found` | Node.js 미설치 | 설치 후 터미널 재시작 |
| 17-4 | 마켓플레이스 추가 실패 / `repository not found` | **PRIVATE 저장소 권한** 또는 주소 오타 | `gh auth status` 로 로그인 확인 |
| 17-5 | 설치는 됐는데 명령이 반만 보인다 | 옛 버전 캐시 | 제거 → 재설치 → **완전 재시작** |
| **17-20** | **마켓플레이스는 추가됐는데 `sodam-graph` 가 목록에 없다** | `marketplace.json` 의 `plugins[0].name` 과 `plugin.json` 의 `name` 이 **다름** | 두 값을 `sodam-graph` 로 맞춤 (`04` §마켓플레이스 규격) |
| **17-21** | **`이미 존재하는 마켓플레이스` 류 메시지** | 다른 소담 형제와 **마켓플레이스 이름이 겹침** | 이 도구는 `sodamgraph-marketplace` 를 씁니다. 겹치면 그 이름을 바꿔 다시 추가 |
| **17-22** | **설치 후 세션을 열어도 자동 안내가 안 뜬다** | `hooks/hooks.json` 누락 또는 경로 하드코딩 | 훅 파일 존재 확인 · 경로가 `${CLAUDE_PLUGIN_ROOT}` 인지 확인 · `SODAM_GRAPH_SILENT` 가 켜져 있지 않은지 확인(§17-8) |
| **17-23** | **`repository not found` / 저장소를 못 찾음** | 저장소가 **비공개**인데 로그인 계정에 권한이 없음, 또는 주소 오타 | `gh auth status` 로 로그인 확인 → 안 되면 **(B) 내 컴퓨터 폴더 방식**으로 설치 |
| **17-24** | **권한/인증을 묻거나 거부당함** | GitHub 인증이 만료·미설정 | `gh auth login` 후 재시도 → 그래도 안 되면 (B) 폴백 |
| **17-25** | **`marketplace.json 을 찾을 수 없음`** 류 | 저장소 **루트**에 `.claude-plugin/` 이 없음 (저장소가 폴더 한 겹 안쪽인 경우) | 저장소 루트를 확인. **소담루프엔지니어링이 실제로 이 구조**(`sodamloop/` 안이 저장소)라 패밀리 안에 선례가 있음 |

> 🔴 **§17-23~25는 `03_PHASES.md` M0 `done_when` 8번(GitHub 설치 실측)에서 실제로 겪은 메시지로 교체하십시오.** 지금 적힌 문구는 **예상 증상**이고, 화면에 뜬 실제 문장이 있어야 사용자가 복사해서 찾을 수 있습니다(§17 규격).

#### 첫 실행 단계

| # | 증상 | 원인 | 해결 |
|---|------|------|------|
| 17-6 | **`경로 유실 — 재탐색 필요` / `lost`** | 형제 폴더가 `search_roots` 밖 | `data/graph.json` 의 `config.search_roots` 를 **자기 폴더 위치로 수정**(복붙 예시 제공) |
| 17-7 | **`coarse (상세 미상)` 이라고 나온다** | 그 프로젝트에 진행 기록 파일이 없음 | **정상입니다.** `/graph-shadow` 로 한 번만 채우면 됩니다 |
| 17-8 | 세션을 열어도 아무것도 안 뜬다 | ① 형제 폴더 밖에서 열었음(정상) ② `SODAM_GRAPH_SILENT=1` 켜짐 | 환경변수 확인 |
| 17-9 | 세션 시작이 **느려졌다** | 폴더가 많아 스캔이 오래 걸림 | `config.scan_filter` 로 범위를 좁힘 |
| 17-10 | 한글이 깨져 `???` 로 나온다 | 터미널 인코딩(UTF-8) | 인코딩 변경 방법(Windows PowerShell 기준) |

#### 사용 중

| # | 증상 | 원인 | 해결 |
|---|------|------|------|
| 17-11 | **완료 제안이 떴는데 아직 안 끝났다** | 자동 판정이 틀림 | `/graph-reject <항목>` — **다시 제안하지 않습니다** |
| 17-12 | **이미 완료 처리됐는데 되돌리고 싶다** | 시간이 지나 자동 승격됨 | `/graph-undo <항목>` |
| 17-13 | `/graph-why` 지목이 이상하다 | ① 스캔이 오래됨 ② `coarse` 라 추정치 | 스캔 시각 확인 후 재실행. `(추정치)` 표시 확인 |
| 17-14 | **다른 창에서 작업했더니 정보가 다르다** | 여러 세션 동시 사용 | **정상입니다.** 출력의 `[HH:MM 기준]` 시각 확인 |
| 17-15 | 명령 이름이 다른 소담 플러그인과 겹친다 | — | 이 도구는 전부 `graph-` 로 시작합니다 |
| 17-16 | `순환 의존 발견` 경고 | 계획이 서로를 기다리는 상태 | 무엇과 무엇이 물렸는지 표시됨 — 하나를 떼어내야 함 |

#### 되돌리기·제거

| # | 항목 | 내용 |
|---|------|------|
| 17-17 | **완전히 지우려면** | 플러그인 제거 → `~/.sodam/graph-state.json` 삭제 → (원하면) 프로그램 폴더 삭제 |
| 17-18 | **`graph.json` 을 잘못 고쳤다** | `git checkout data/graph.json` 으로 되돌리기(명령 제공) |
| 17-19 | **형제 프로젝트가 바뀐 것 같다** | **이 도구는 쓰지 않습니다.** 확인 명령 제공 후, 그래도 다르면 다른 세션이 작업한 것 |

> **규격**: 위 **25개**(기존 19 + 마켓플레이스 3 + GitHub 설치 3)는 **최소**입니다. 구현 중 실제로 겪은 오류를 **전부 추가**하십시오.

### §18 FAQ — 최소 12문항

1. 이걸 쓰면 제 프로젝트가 바뀌나요? → **아니요, 읽기만 합니다**(확인 방법 포함)
2. 인터넷 연결이 필요한가요? → 아니요
3. 돈이 드나요? → **0원.** 서버·계정·구독 없음
4. 다른 컴퓨터에서도 되나요? → 지금은 이 PC 기준. 다른 PC는 다음 단계
5. 소담 형제가 아닌 프로젝트도 볼 수 있나요? → 지금은 7형제만
6. `coarse` 가 뭔가요? → "대충은 아는데 자세히는 모르는 상태"
7. 완료 제안을 무시하면? → **다음 스캔에서 완료 처리됩니다**
8. 여러 창을 켜도 되나요? → 됩니다. 시각 표시를 확인하세요
9. 폴더 이름을 바꿔도 되나요? → **됩니다.** 이름이 아니라 저장소 주소로 찾습니다
10. 저장하는 정보에 비밀번호가 들어가나요? → **아니요** (`08` S-4)
11. 상업적으로 써도 되나요? → **됩니다** (Apache-2.0, `09` §9)
12. 문제가 생기면 어디에 물어보나요? → 저장소 이슈 링크

### §19 법률·저작권·라이선스·상업적 용도

`09_LEGAL_LICENSE_SPEC.md` **L-5 표를 그대로** 옮깁니다:
라이선스 · 저작권자 · **할 수 있는 것** · **지켜야 할 것** · **하면 안 되는 것** · **책임 제한(보증 없음)** · **외부 자료 사용 주의** · 상업적 사용 가능 여부

> **과장 금지**: "완전히 안전"·"법적으로 문제없음" 표현 금지 (`09` L-5, 하네스 `SODAM_FAMILY_COEXIST.md` §7).

---

## 3. 문체 규칙 (전 섹션 공통)

| 규칙 | 예 |
|------|---|
| **전문 용어에는 괄호 설명** | "저장소(프로젝트 폴더)" · "훅(자동 실행되는 작은 프로그램)" |
| **명령은 통째로 복붙 가능하게** | 줄바꿈·설명을 명령 줄 안에 섞지 않음 |
| **"~하세요" 로 통일** | 반말·수동태 금지 |
| **한 문단 3줄 이내** | 벽 같은 글 금지 |
| **경로는 실제 예시로** | 단 **사용자명·개인정보는 마스킹**(`09` L-6.4) |
| **스크린샷 대신 텍스트 출력 예시** | 이미지 자산을 만들지 않음(`09` §5 — 권리 문제 0 유지) |
| **형제 이름 축약 금지** | "루프" ❌ → "소담루프엔지니어링" ✅ (`04` 규칙) |

---

## 4. 문서 세트와 위치

| 문서 | 필수 | 시점 |
|------|------|------|
| **`README.md`**(한국어) | **Must** | M8 |
| `LICENSE` · `NOTICE` | **Must** | M0 (`09` L-2) |
| `README.en.md` | Could | PUBLIC 전환 시 (`09` L-10) |
| ~~`GUIDE.md`~~ | **만들지 않음** | 2026-07-27 형제 6곳 공통으로 GUIDE 제거(실측, 가정 7) |

> 🔴 **GUIDE를 새로 만들지 마십시오.** 형제 6곳이 **같은 날 GUIDE를 제거하고 README로 통합**했습니다. 7번째만 만들면 패밀리 일관성이 깨집니다.

---

## 5. M8 수용 기준 (`03_PHASES.md` 편입)

- [ ] **필수 목차 19개 섹션 전부 존재**
- [ ] **§17 문제 대처 최소 25개 항목** (기존 19 + 마켓플레이스 3 + GitHub 설치 3) + 구현 중 실제로 겪은 오류 추가
- [ ] 🆕 **§5~§6 설치 절차가 `04_PROJECT_SPEC.md` §설치 방법 7단계와 일치** (두 문서가 어긋나면 왕초보가 중간에 막힙니다)
- [ ] **§18 FAQ 최소 12문항**
- [ ] `<details>` 토글로 업데이트 요약 작성
- [ ] 아키텍처 그림 1장 이상
- [ ] 모든 명령에 **실제 출력 예시** 1개씩
- [ ] `09` L-5 라이선스 안내 7항목 포함
- [ ] **사용자명·실제 개인정보 미노출**(`09` L-6.4)
- [ ] 🔴 **처음 보는 사람이 README만 보고 설치 성공** — **새 세션에서 실제로 시도해 확인**

---

## 6. [NEEDS CLARIFICATION]

- [ ] 저장소가 PRIVATE이면 §18 FAQ 12번(이슈 링크)을 어떻게 안내할지
- [ ] 설치 방법을 마켓플레이스 기준으로 쓸지, `git clone` 기준으로 쓸지 (`09` D-1 공개 범위 결정에 종속)
