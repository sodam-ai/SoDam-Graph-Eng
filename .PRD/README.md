# 소담그래프엔지니어링 (SoDam-Graph-Eng) — 설계 문서

> Show Me The PRD로 생성됨 (2026-08-02) · **7차 보강 완료 · 착수 준비 완료**
> 소담 7형제 중 **일곱 번째**

## ✅ 이 PRD는 착수 가능합니다

| 회차 | 발견 결함 | 등급 |
|------|----------|------|
| 2·3차 | 경로 하드코딩 · remote 실측값 오류 · 정본 미인지 | **치명** — 구현하면 도구가 목적을 파괴 |
| 4·5차 | `미상` 표기 · `done_when` 검수 약함 · `06` 누락 | 중 — 설계 무력화 |
| 6차 | 가정 2 중복 · 건수 불일치 | 경미 — 문서 내부 표기 |
| **7차** | **`graph.json` 최상위 구조 부재 · 픽스처 규격 부재 · Mermaid 타입 미정** | **착수 블로커** — 문서 정합성 검사로는 안 잡히는 유형 |
| 8·9차 | 보안(`08`) · 법률(`09`) · README(`10`) 규격 부재 | 중 — 핵심 요구사항 누락 |
| **10차** | **정지 데이터가 반나절 만에 무효 · 루프 추적파일 오기재 · `~/.sodam` mkdir 부재** | **착수 블로커** — `done_when` 을 충족 불가능하게 만듦 |
| **11·12차** | **`marketplace.json`·`hooks.json` 부재 · 설치 명령 형식 부재** | **착수 블로커** — 다 만들어도 **설치가 안 됨** |
| **13차** | **가정 원장 건수 오기(35/51↔실제 95) · 미결 건수 오기(1↔3) · `06` 내부 충돌 · M-V D가 신설 규격 미검사** | 경미(모순·오기) — **단 M-V D는 다음 개정을 또 놓치게 하는 유형** |

> 🔴 **6차의 "개정 종료" 판단은 성급했습니다.** 그때는 **문서 내부 정합성만** 검사했고, **"이 문서로 코드를 짤 수 있는가"** 는 보지 않았습니다. 7차에서 그 각도로 보니 착수 블로커 4건이 나왔습니다.
> → **M-V에 "D) 구현 가능성 검사"를 신설**해 같은 오판을 막습니다.

> **다시 개정하는 조건은 3가지뿐**: ① **착수를 막는 규격 공백이 구현 중 실제로 드러났을 때** (13차에서 *"구현 전후 무관"* → 이렇게 좁힘) ② 남은 결정 3건 중 하나가 뒤집힘 ③ 2주 후 측정 미달.
> 그 외 문서 결함은 **[`03_PHASES.md` §M-V](./03_PHASES.md) A·B·C·D가 잡습니다.**

> 🛑 **13차 (2026-08-02) — 개정을 멈추고 구현으로 넘어갑니다.**
> **근거**: 문서 **3,656줄** vs 코드 **145줄**(25배), 그런데 **M0조차 부분 완료**입니다. 오늘 하루에 10·11·12차 세 번 개정했고 **전부 조건 ①의 "구현 *전*"으로 발동**했습니다.
> 이 PRD는 지금 `verified`(설계 검증됨)에서 `done`(구현 착수)으로 못 넘어가는 상태 — **[`06`](./06_ANTI_STALL_SPEC.md) 이 잡으려는 통증 ③ 바로 그 증상**입니다. 그래서 조건 ①을 좁혔습니다.

---

## 한 줄로

소담 7형제의 **위치·진행단계·의존관계를 그래프 파일 하나로** 만들어, 세션을 새로 열 때마다 AI가 "지금 어느 형제의 어느 단계이고, 다음 할 일은 이것"을 즉시 알게 하고, **그 결과를 6형제가 함께 읽는** Claude Code 플러그인.

---

## 문서 구성

| 문서 | 내용 | 언제 읽나 |
|------|------|----------|
| [01_PRD.md](./01_PRD.md) | 뭘 만드는지, 왜, 성공 기준 + **가정 원장 95건** | 프로젝트 시작 전 |
| [02_DATA_MODEL.md](./02_DATA_MODEL.md) | `graph.json` 구조 (**정본/파생 분리 · 2층 추출 · 공유 발행**) | 데이터 설계할 때 |
| [03_PHASES.md](./03_PHASES.md) | Phase 1(M0~M11) / 2 / 2.5 / 3 + **시작 프롬프트** | 개발 순서 정할 때 |
| [04_PROJECT_SPEC.md](./04_PROJECT_SPEC.md) | AI 규칙 / "절대 하지 마" 25개 | **AI에게 코드 시킬 때마다** |
| [05_RESEARCH_MAPPING.md](./05_RESEARCH_MAPPING.md) | 조사 1,485줄 + **형제 계약 문서** 채택/보류/제외 | 도구를 고를 때마다 |
| [06_ANTI_STALL_SPEC.md](./06_ANTI_STALL_SPEC.md) | **정체 해소 규격** — 이 프로젝트의 존재 이유 | **Phase 1 구현 내내** |
| [07_FAMILY_COEXIST.md](./07_FAMILY_COEXIST.md) | **7형제 공존 (정본 종속)** + 발견한 불일치 3건 | 형제와 얽히는 모든 작업 |
| [08_SECURITY_SPEC.md](./08_SECURITY_SPEC.md) 🔒 | **보안 스펙** — ASVS·Secure by Design, Must/Should/Could | **M2 구현 내내 · 외부 도구 도입 전** |
| [09_LEGAL_LICENSE_SPEC.md](./09_LEGAL_LICENSE_SPEC.md) ⚖️ | **법률·라이선스** — Apache-2.0 확정, 확인된 사실/검토 필요 분리 | **M0 · 공개·납품·판매 전** |
| [10_README_SPEC.md](./10_README_SPEC.md) 📘 | **README 작성 규격** — 왕초보 기준, 필수 목차 19개 | **M8** |
| [RESEARCH_SOURCES.md](./RESEARCH_SOURCES.md) | Graph Engineering 생태계 조사 (사용자 작성, 1,485줄) | 05번과 함께 |

### 🔴 이 문서들보다 먼저 읽어야 하는 것 (형제 저장소, 읽기 전용)

| 순위 | 문서 | 지위 |
|------|------|------|
| 1 | `SoDam-Agentic-Eng/docs/family-synergy.md` | **패밀리 정본** (**6,926B · 2026-08-02 11:35 갱신** — 읽기 직전 수정 시각 재확인 필수) |
| 2 | `SoDam-Harness-Eng/.PRD/SODAM_FAMILY_COEXIST.md` | 하네스 관점 초기 메모 (7,467B) |

**충돌 시 우선순위**: 정본 > 하네스 메모 > `07` > 나머지.

---

## 확정된 결정 11건

| 결정 | 선택 | 뜻 |
|------|------|-----|
| Phase 1 범위 | 지도 + 다음 할 일 + 정체 해소 | 알려주는 데까지. 실행은 안 함 |
| 형제 경계 | **그래프=판정 / 루프=실행** | 역할 안 겹침 |
| 저장 방식 | **파일 (JSON + Mermaid)** | 서버·DB·계정·비용 0개 |
| 형제 저장소 | **읽기 전용** | 6형제 파일은 한 글자도 안 건드림 |
| 지도 범위 | **엔지니어링 7형제만** | Persona·O-Brain·WikiMate 등 제외 |
| 초기 데이터 | **2층 추출** | 1층 공통분모 자동 + 2층 그림자 |
| **식별 열쇠** | **`repo_remote` + `markers`** | 경로를 정본에 안 씀 → **폴더 개명에 안 깨짐** |
| **공유 발행** | **`~/.sodam/graph-state.json`** | 6형제가 지도를 읽어감 ← **시너지의 실체** |
| 저장소 이름 | **`sodam-ai/SoDam-Graph-Eng`** | 6형제 명명 규칙 실측 일치 |
| 🆕 **완성 형태** | **마켓플레이스 + 플러그인** — `sodam-graph` / `sodamgraph-marketplace` ⬜ | 형제 6/6이 `marketplace.json`+`plugin.json` 둘 다 보유(실측). 마켓플레이스 이름은 사용자 확정 대기 |
| 🆕 **설치 표준** | **GitHub 마켓플레이스** (로컬 폴더는 폴백) | **2026-08-02 사용자 확정 — 7형제 전체 통일.** ⚠️ **PRIVATE 저장소 설치 가능 여부는 M0에서 실측 예정**(패밀리 검증 기록 0건) |

---

## 해결하려는 문제 3가지

| # | 문제 | Phase 1에서 |
|---|------|-----------|
| ① | 형제마다 폴더 구조가 달라 AI가 경로를 헷갈림 | **설계됨** — `repo_remote` 로 찾고 폴더 개명에도 안 깨짐 |
| ② | 세션 바꾸면 진행 상황을 처음부터 다시 설명 | **설계됨** — 세션 시작 자동 주입 (3줄, 1초) |
| ③ | 검증 끝났는데 다음 단계로 안 넘어감 | **설계됨** — `done` 자동 승격 + `/graph-why` + 그림자 ([06번](./06_ANTI_STALL_SPEC.md)) |

> 🔴 **"해결"이 아니라 "설계됨"입니다.** 코드가 아직 0줄이므로 해결 여부는 [`01_PRD.md §5`](./01_PRD.md) 의 **2주 후 측정 3항목**으로만 판정합니다.
> 근거: 하네스 `SODAM_FAMILY_COEXIST.md` §7 — *"100% 안전" 표현은 6개 프로젝트 공통 금지*.

---

## 🔎 이 PRD가 코드 없이 이미 찾아낸 것 3건

> 도구가 하려던 일(형제 간 불일치 발견)을 **설계 단계에서 먼저 해냈습니다.** 존재 이유의 직접 증거입니다.

| # | 발견 | 근거 |
|---|-----|------|
| ① | **설치 순서가 두 계약 문서에서 뒤바뀜** | 정본 §2 `Harness→Loop→Context` vs 하네스 §5 `Harness→Context→Loop` |
| ② | **소담프롬프트엔지니어링 역할이 폐기된 형태로 기재** | 두 문서 모두 "Next.js 웹앱" — 실제는 `CHECKPOINT.md` 1행 **"웹앱 폐기, 플러그인 전용"** |
| ③ | **명령어 중복 금지 규정이 이미 6건 위반** | `install`·`status`·`undo`·`checkup`·`log`·`start` 가 형제 간 중복 (실측) |

그리고 **자기 계획 안의 순환**도 스스로 잡아냈습니다 — Phase 2가 가장 오래 정지한 형제에 의존하던 구조를 Phase 2.5로 분리했습니다([`06` §3](./06_ANTI_STALL_SPEC.md)).

---

## 다음 단계

[03_PHASES.md](./03_PHASES.md) 의 **"Phase 1 시작 프롬프트"** 를 복사해서 **새 세션**에 붙여넣으세요.

**착수 전 확인 6가지:**

```
[ ] 정본 family-synergy.md 읽기 (SoDam-Agentic-Eng/docs/)
[ ] 하네스 SODAM_FAMILY_COEXIST.md 읽기
[ ] M0 저장소 생성 (git init + GitHub SoDam-Graph-Eng)
[ ] 결정 3건 (아래 미결 사항)
[ ] 개명 테스트는 _test_fixture/ 더미로만 — 실제 형제 폴더 금지
[ ] 만들 파일 목록을 한 번에 선언하고 시작 (게이트 마찰 최소화)
```

---

## 미결 사항 — **3건** (전부 착수를 막지 않음)

**해소됨 (2026-08-02 사용자 결정)**

- [x] **설치 순서 불일치** → **어느 쪽도 채택하지 않고 M8 보고서에 발견 사실만 기록**. 소담그래프엔지니어링은 독립 트랙이라 자기 동작에 영향 없음
- [x] **`.PRD` git 추적 여부** → **추적함** (실행 완료 — 커밋 `a02f7cf`)
- [x] **설치 표준** → **GitHub 마켓플레이스** (로컬 폴더는 폴백). 7형제 전체 통일

**남은 것 — 셋 다 "기본값으로 진행 + 나중에 한 곳만 변경"**

| # | 항목 | 기본값 | 바꿀 때 고칠 곳 |
|---|------|-------|--------------|
| 1 | `SoDam-Agentic-Eng` 한글 정식 명칭 | `소담에이전틱엔지니어링` (폴더·플러그인·remote 3중 일치) | `data/graph.json` 의 `name_ko` **한 줄** |
| 2 | 마켓플레이스 이름 | `sodamgraph-marketplace` (실측 다수파 3/6) | `.claude-plugin/marketplace.json` 의 `name` **한 줄** |
| 3 | 저장소 공개 범위 | **PRIVATE 유지** | **M0 `done_when` 8 실측 결과에 종속** — PRIVATE로 GitHub 설치가 되면 이 결정 자체가 불필요. 안 되면 [09 §10-B](./09_LEGAL_LICENSE_SPEC.md) 사전 처리 후 사용자 결정 |

> 🔴 **"1건"이라고 적어두면 나머지를 빠뜨립니다.** 실제 `⬜` 대기 항목을 전부 한 표에 모았습니다.

### 2·3차 개정에서 해소된 미결 (재질문 불필요)

| 항목 | 결론 | 근거 |
|------|------|------|
| 저장소 이름 | `sodam-ai/SoDam-Graph-Eng` | 6형제 remote 실측 |
| 명령어 접두사 | `graph-` | 충돌 0건 실측 |
| 세션 자동 주입 | 켬 + 3줄 + 1초 + 끄는 법 | 통증 ②가 세션 전환 |
| 불일치 대응 | 경고 + 승격 제안 | `06` §1 |
| `days_since_activity` | git 커밋 + 파일 시각 **둘 다** | 루프는 커밋이 안 잡힘 |
| 초기 데이터 | 2층 추출 | 형식 4가지 실측 |
| 지도 범위 | 엔지니어링 7형제만 | 사용자 확정 |

| `coarse` 점수 반영 | **신뢰도 0.7 + `(추정치)` 표시** | 7형제 중 3개가 `coarse`, 그중 최장 정지 형제 포함 |
| Phase 2.5 M16 | **"인계" → "왕복"** 재정의 | 인계는 공유 발행으로 Phase 1에서 이미 달성 |

| 저장소 이름 | **`sodam-ai/SoDam-Graph-Eng`** | 소문자로 만들면 M0 즉시 실패 |

**가정 원장** — [01_PRD.md §8](./01_PRD.md) 에 **95건** (1~16 초안 · 17~49 2~5차 · 50~51 6차 · 52~56 7차 · 57~64 8차 · 65~74 9차 · 75~80 10차 · 81~88 11차 · 89~95 12차).

> 🔴 **95건을 다 읽고 구현하지 마십시오.** 구현자의 정본은 [03_PHASES.md](./03_PHASES.md) 의 **"Phase 1 시작 프롬프트"** 입니다. 가정 원장은 *"왜 그렇게 정했나"* 를 되짚을 때만 봅니다.

---

## 🚀 착수 절차 (이대로 하시면 됩니다)

```
1. 새 세션을 연다
2. SoDam-Agentic-Eng/docs/family-synergy.md 를 읽는다   ← 정본, 첫 작업
   (읽기 직전에 파일 수정 시각을 확인 — 세션 중에도 바뀝니다)
3. 03_PHASES.md 의 "Phase 1 시작 프롬프트" 를 붙여넣는다
4. M0 → M1 → M2 → … → M8 순서로 진행
5. 각 M 완료 시 done_when 충족 증거를 보고받는다
```

### ✅ 진척 (2026-08-02)

| 마일스톤 | 상태 | 증거 |
|---------|------|------|
| **M0** 저장소 준비 + 매니페스트 | 🟡 **부분 완료** | 커밋 `a02f7cf` · `done_when` **1~5 통과**(GitHub 인식 `apache-2.0` · 시크릿 0건) · **6~8 미완**(매니페스트 2종 + **GitHub 설치 실측**) |
| **M1** `graph.json` 정본 | ✅ 완료 | 커밋 `680dc87` · `done_when` 5/5 + 무결성 7개 · 드라이브 경로 1건만 |
| **M2** 스캐너·resolve·발행 | 진행 전 | `lib/gitSafe.mjs` 작성·검증 완료(미커밋) |

🔴 **M1 초기 데이터는 이 문서의 실측 표가 아니라 착수 시점 재실측(13:30)으로 채웠습니다** — `01_PRD.md §1` 의 "착수 시점 재실측" 블록이 최신 정본입니다.

**착수 블로커 0건.** 남은 결정 3건은 셋 다 착수를 막지 않습니다 — 1번은 `graph.json` 한 줄, 2번은 소담그래프엔지니어링이 어차피 어느 쪽도 채택하지 않음, 3번은 첫 커밋 직전에 정하면 됩니다.
